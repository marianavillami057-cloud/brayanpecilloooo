kind = "web"
previewPath = "/"
title = "Alejandro Pecillo - Construcción"
version = "1.0.0"
id = "artifacts/construccion"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21339

[services.development]
run = "pnpm --filter @workspace/construccion run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/construccion", "run", "build" ]
publicDir = "artifacts/construccion/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21339"
BASE_PATH = "/"
